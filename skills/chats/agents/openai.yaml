interface:
  display_name: "Chats"
  short_description: "Search every past coding-agent session with cass"
  default_prompt: "Use $chats to search past agent sessions for this."
policy:
  allow_implicit_invocation: true
