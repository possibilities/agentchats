# Decision log

[0001: Own the session index](0001-own-the-session-index.md) is the current
index-ownership decision. Read it with [CONTEXT.md](../../CONTEXT.md) before
changing native readers, indexing or query behavior.

## Withdrawn viewer experiment

The earlier `0001-viewer-on-vendored-opencode-renderer.md` belonged to a
transcript-viewer experiment explicitly withdrawn on 2026-08-18 in
[commit 76965ba](https://github.com/possibilities/agentchats/commit/76965baed07531023115d12d085bcf169e0c86e4).
It is unrelated to the current record that later reused `0001`. The commit
retains the renderer tradeoffs as historical evidence; it does not describe an
active AgentChats viewer or an instruction to restore that feature.

Do not reuse an identifier for an unrelated decision. Cite the complete record
filename, and preserve a changed choice with an explicit replacement link.
